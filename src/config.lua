local config = {
  enabled = true
  InstantOmegaCast = {
    Enabled = true
  }
}

local description = {
  enabled = "Set to true to enable the mod. Set to false to disable the mod."
  InstantOmegaCast = {
    Enabled = "Enable to make the Omega cast fire instantly when you press the cast button. Will fire basic cast if mana is insufficient."
  }
}

return config, description